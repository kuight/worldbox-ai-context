# Z-TASKBOOK — Z 段任务书（逐字副本）

> ★ 本文件为 `docs/SESSION-LOG/2026-09-22-R4-V.md` **第 643~767 行**的逐字副本，**以原文件为准**。
> 复制动作：只读抽取，未修改原 SESSION-LOG。
> 段字节数 9629｜段 SHA-256 `61fb1186be79d5cee420d37e6b68f5ad7ce7f417bbe93a04fe89ed255bf454e0`
> 与 `回执.txt` 附录 A 所记 SHA-256 `61fb1186be79d5cee420d37e6b68f5ad7ce7f417bbe93a04fe89ed255bf454e0` 一致：**是**

---

#### Z 段任务书全文（原文照录）
```
任务书 R4-V · Z 段（冻结失效诊断）  2026-09-23
★ 本文以对话侧原始 Z 段文本为准；Z-0 与 Z-1b 为后补，不改动原有编号。
镜像基线：8bd487803b70ddda8f8e3b51646f8640770f1606
本地基线 SHA：执行侧自查回填（参谋侧无法核实）
本段停点：Z-0.6（归档推送后停报），随后 Z-1 由用户执行。
本任务书覆盖此前一切未执行的 W / X / Y 段待办指令。

【Z-0 归档与状态对齐（后补，本轮唯一落盘动作，只写 docs，不碰 src）】
Z-0.1 把本任务书全文原样补入 docs/SESSION-LOG/2026-09-22-R4-V.md 中
      【Z 段前置 · 未决假说与裁决】S-1.5(d) 的占位处。逐字，不摘要不改写。
Z-0.2 ★修复执行侧状态不一致（最高优先）★
      现状：STATE-BRIEF.md Z0 节已写「停点 Z-1」，但 RUNNER-HANDOFF.md
      §1 仍写「当前停点 = W-0.6」、§3 仍写「停在哪：K-7.4」。执行侧换对话
      只读 RUNNER-HANDOFF ⇒ 会误按 W 段恢复，而 W 段裁决 B-3 含造墙预授权。
      改法：RUNNER-HANDOFF §1 开头加置顶行——
        「★★ 当前任务书 = Z 段；停点 = Z-0.6；阻塞项 = B-21R；
          未决假说 = S-1.1 / S-1.2；**W 段 B-3 造墙预授权即日起撤销**，
          任何 .造 / .打 / invoke_power 类动作须参谋侧逐次放行。」
      §3 同步改为「停在哪：Z-0.6」。旧行标注作废，不删除。
Z-0.3 STATE-BRIEF.md「存档缺口」：(d) Z 段任务书全文 → 已到达并落盘（记墙钟）；
      S-3 的 Z-1 / Z-6 用户回答 → 仍为占位。
Z-0.4 一致性核对（实测优于任务书）：本任务书任一事实性断言若与已落盘内容不符，
      如实列差异，不得为迎合任务书改动已落盘文字。特别核对本地 SHA、pytest 计数、
      8bd4878 是否确为镜像最新 HEAD。
Z-0.5 不改 src、不改 DB、不重启 bot、不发任何 /cmd。
Z-0.6 提交推送，message：
      R4-V: land Z task book, revoke W-phase build pre-authorization
      回报 ≤15 行：本地 40 位 SHA、镜像 40 位 SHA、RUNNER-HANDOFF / STATE-BRIEF /
      SESSION-LOG 定长直链、Z-0.4 差异清单（无则写「无差异」）。然后停报。

【Z-1 视觉基准核对（用户执行，执行侧只记录）★最优先，先于一切推断】
由用户切到游戏窗口，肉眼回答四问，执行侧原样记录并附墙钟：
 1) 单位（人/动物）是否在移动？
 2) 昼夜 / 天气 / 云影是否在变化？
 3) 界面暂停按钮当前是按下态还是弹起态？速度档位显示为几？
 4) 界面上若有年份或世界时间显示，读数是多少？隔 60 秒再读一次，是否变化？
同时执行侧在用户观察的同一分钟内取一次 get_world_state，贴 tick 与 paused。
★ 判据：
  情形 P 屏幕在动 → tick 为真，世界确在运行，进 Z-2。
  情形 Q 屏幕静止而 tick 仍以约 59/s 增长 → tick 不是世界时间（疑为帧计数），
    D-033 判据整条作废，改用 world_time / 年份 / population 等语义量重建判据（见 Z-1b），
    并重估此前所有「世界在跑」的结论（含 ENV-1、C-3、B-21R）。此情形下停报等裁决。
★ 本步允许用户点击游戏窗口，这是唯一被授权的一次窗口交互，须记录时刻。

【Z-1b 语义量交叉验证（后补；仅情形 Q 触发，落地 Z-1 中「语义量重建判据」）】
Z-1b.1 间隔 ≥300 秒取两次只读快照，每次含 8724 list_cities_ex 全量原文、
       8723 get_world_state 原文、8723 list_kingdoms 原文，均记墙钟毫秒与 tick。
Z-1b.2 逐项比对：各城 population、building_count、王国数、kingdoms_ever_created、
       population_alive、界面年份（由用户读）。
Z-1b.3 语义量有变化 ⇒ tick 仍可能为世界时间，回到情形 P 处理；
       语义量逐字不变而 tick 增长 ≥15000 ⇒ S-1.1 成立，立即停报等 D-033 存废裁决。
Z-1b.4 本步只读，禁止任何写世界命令（含 pause / resume / set_speed）。

【Z-2 优雅重启游戏（情形 P 才做）】
依据：13:30 pause 成立（睡前），18:03 唤醒后历次冻结全部失败；同一进程 PID 5464
自 09-21 19:30:10 运行至今。怀疑 S3 挂起 / 恢复破坏了 Unity 侧时间或暂停状态机。
 a) 重启前先让游戏自行存盘：由用户在游戏内手动保存到 save10，报存盘完成时刻。
 b) 执行侧记录 saves\save10 四件最小文件集的 size / mtime / SHA256（存盘前后各一次）。
    四件：map.meta、map.wbox、map_stats.s3db、EmpireCraftModData.json（D-037 最小集）。
 c) 用户在游戏内正常退出（禁止强杀进程，AGENT-CONTRACT 禁止项 3）。
 d) 重开游戏、载入 save10，报新 PID 与启动时刻。
 e) 载入后先取一次 get_world_state，贴 tick / paused 作为新基线。

【Z-3 重启后冻结重测（执行侧发 API pause，不再劳烦手动）】
依据 C-3d：pause 为 0.4.0 原生命令，与手动暂停等效，且手动方式已试过无效，
改用 API 可把发令时刻精确到毫秒、便于对齐。
 a) 发 pause，贴请求与响应原文（含 previous_paused / paused）与墙钟。
 b) 立即按 Y-3.2 协议采样：每 15 秒一次、连读 9 次、≥120 秒，贴九次全表
    （墙钟到毫秒 / tick / 累计 /cmd 次数 / paused 字段）。
 c) 延长观测：采样通过后继续每 60 秒读一次，连读 12 次（覆盖约 12 分钟），
    全程 tick 不得按时间增长。理由：13:30 那次正是在第 10 分钟前后失守，
    120 秒窗口不足以复现。
 d) Y-3.2b autosave 佐证照做，并加一条判定：若世界确已冻结而 autosave 仍按 301 s
    节拍产生新槽，则 autosave 为实时驱动、不能作为运行判据，登记并移出判据集。
 e) 三情形判定与 B-22 登记规则沿用 Y-3.2 原文
    （甲 tick 全同 = 冻结；乙 每命令 +1 = 冻结且步进，登记 B-22；丙 按时间增长 = 在跑）。
 f) 12 分钟全程冻结 → 记 TF，直接进 Y-3.3~Y-3.7，不必等我回话。
    中途失守 → 停报，并贴失守前最后一次与失守后第一次的墙钟、tick、paused。

【Z-4 若重启后仍无法冻结】
登记 B-21R 升级为阻塞项，只读采集以下佐证后停报：
 1) 游戏内速度档位与暂停按钮的实际状态（用户肉眼）；
 2) BepInEx\config\WorldBoxBridge.cfg 全文（凭据掩码）；
 3) EmpireCraft 是否有独立的时间 / 模拟驱动的线索：在其源码目录 grep
    「timeScale」「Update(」「pause」「Paused」的命中计数与代表性行号
    （只报计数与行号，不贴大段源码；0 命中附正样本）。
    理由：EmpireCraft 为 1531 文件的重型模组，其自有模拟循环是否受游戏暂停约束未经验证。
 4) 不要为此改动任何模组或游戏文件。

【Z-5 代码段解耦放行（与 Z-1~Z-4 并行，已批准）】
 a) Y-4 全部代码修复照常进行，不依赖冻结。解冻范围仅 src\qqbot\resolver.py、bot.py、
    broadcaster.py、tests\*；禁改 approval.py、ledger.py、executor.py、adapter.py、
    catalog.py 及 X-2 禁改清单其余文件。内容：own 路径按 kingdom_id 过滤（D-032）、
    错误码三分 NOT_FOUND / UNSTABLE / CITY_GONE（B-17）、B-19 空白归一化
    （U+200A 等；归一化只用于比较，回执显示桥返回原名）、B-14 区分 None 与 0、
    .bind dead 通道（D-036，state dead→active，cost 0，记 ledger_log）。
 b) Y-5 fixture 允许改用「非冻结态、同一世界」的实采原文，按 K-5.1 同法逐字节落库，
    每份标注采集墙钟与当时 tick，文件头注明
    「非冻结态采样，仅供解析逻辑回归，不得用作世界状态基准」。
 c) Y-5 用例中凡涉及 KB 的常量，暂用采集时城 id 6 的实际 kingdom_id，
    并在注释写明该值随世界演化会变，联机段以 Y-3.5 实读为准。
 d) Y-6 验收照旧：pytest 连跑 3 次 ≥173 全绿、diff 限解冻清单、
    对账门 resolver.py:173-204 逐字未动、提交回 40 位 SHA。
 e) Y-7 联机段整体推迟到冻结成功之后，不得在世界运行态下执行造墙。
    理由：目标城 building_count 会被自然建造污染，无法归因（ARCH §5.4 对账门本意）。
 f) B-19 复核必须用 repr() 或逐字节输出，回执文本里空白看着正常不算证据。

【Z-6 需用户补充的记忆（写入 SESSION-LOG，用于 B-21R 定性）】
请用户回忆并回答 20:30~21:00 之间点暂停的具体操作顺序——用的是界面暂停按钮还是
空格键？点了几次？点完之后有没有再点回游戏窗口、有没有拖动过地图或打开过任何面板？
记不清就写「记不清」，不要补齐，执行侧按原话记录，不得代为推测。

【停报项：沿用 Y-8 的 a~k，另加】
 l) Z-1 落入情形 Q（tick 非世界时间）
 m) Z-3 十二分钟延长观测中途失守
 n) 发现任何在本轮之前、未经参谋侧逐次放行而执行过的改世界动作

【硬约束（沿用 Y 段第九节全文）】
凭据零回显；不手改 DB；不强杀游戏进程（用 CloseMainWindow）；不装计划外组件；
不删任何备份；不改 X-2 禁改清单文件；输出不用省略号；grep 0 命中必须附正样本；
实测优于本任务书断言，不符须如实报告；完整回执落盘 回执.txt，
对话内只回 ≤15 行摘要 + 40 位 SHA + 定长直链。
```
