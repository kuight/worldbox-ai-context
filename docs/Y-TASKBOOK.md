# Y-TASKBOOK — Y 段任务书（原文拆档结果 + 补发原文）

> ★ 本文件按 Z-5 续任务书 Z-5.1 建立：要求把「SESSION-LOG 中 Y 段任务书原文
> （至少含 Y-3.3~Y-3.7、Y-7、Y-8 a~k）」逐字复制为本文件。
> **首次检索结果：未找到**（见 §1~§4，按 Z-5.1 明文「找不到原文就注明『未找到』，
> **不得重建**」，当时不包含任何重建或回忆性复写内容）。
> **后续补发：已收到**（见 §5「补发原文」）。
>
> ★★ 来源标注（按补发指令要求，二选一后如实注明）：
> **「用户自旧对话复制」** —— 该文本由**用户**从旧的参谋侧对话中复制回传；
> 文本在旧对话中的**产出方为当时的参谋侧 AI**（即「旧对话 AI 引用」所指的那一方）。
> 执行侧**原样落盘、一字未改**，未做任何校订、补全或格式化。

## 1. 结论（首次检索）
**Y 段任务书原文未在本机任何落盘位置找到**（既不在 `docs/` 内，也不在任何本地回执中）。
执行侧**不重建、不复写、不凭上下文补写**该任务书。（§5 为后续补发，非本次检索所得。）

## 2. 检索证据（命令模式与命中，逐条照录）
检索时间：2026-09-25 11:5x（Z-5 续执行期）。
检索范围：`docs/SESSION-LOG/*.md`、`docs/*.md`（本地仓）与
`<USER-DIR>\{完成汇报-R4V-J1.txt, 完成汇报.pre-R4V-R4-2i.txt,
完成汇报-R4V.txt, 回执.txt}`。

| 检索模式 | docs 内命中文件 | 本地回执内命中 |
|---|---|---|
| `Y-3\.4` | **0 个文件** | 0 |
| `Y-7\.1` | **0 个文件** | 0 |
| `Y-8 a~k` | **0 个文件** | 0 |
| `Y-7 联机验收` | **0 个文件** | 0 |
| `Y-3 冻结与基线` | **0 个文件** | 0 |
| `Y-5 回归用例` | **0 个文件** | 0 |
| `^═══.*Y 段任务书|Y 段任务书（完整版）|Y 段任务书（合并终版` | **0 个文件** | 0 |

**正样本自验（证明同批 grep 有效）**：
- 同一批 grep 中 `Z 段任务书全文` 命中 `docs/SESSION-LOG/2026-09-22-R4-V.md` ✅
  （该串确实存在于 Z 段归档处）；
- 同一批 grep 中 `Y-3\.3` 命中 `完成汇报-R4V-J1.txt` **1 次**、命中 `docs/SESSION-LOG/2026-09-22-R4-V.md`
  等文件（均为**散文引用**，例如 Z 段补丁 P3 里「12 分钟全程冻结后不进 Y-3.3~Y-3.7」），
  **不是任务书原文段落**。
⇒ 0 命中判定成立；「未找到」是实测结论，不是检索失败。

## 3. Y 段内容的实际落盘位置（供参谋侧取用，非任务书原文）
Y 段任务书的**执行结果**已按轮次落在下列位置（**这些是结果与回执，不是任务书原文**）：
- `docs/SESSION-LOG/2026-09-22-R4-V.md`：【Z 段】Z-0.7（用户口述）、Z-3 相关各节，
  以及 W-0 节中关于 V-3/Y 段基线的引用；
- `docs/RUNNER-HANDOFF.md` §1：V-3/W/X/Y/Z 各段的停点与实测差异摘要；
- `docs/STATE-BRIEF.md` Z0 节：当前状态与判据演进的摘要；
- 本地（**未入镜像**、仅存于本机）：`<USER-DIR>\完成汇报-R4V-J1.txt`
  等历轮回执。

## 4. 影响与建议（首次检索时）
- 影响：Z-5.2「按原文执行 Z-5 代码修复」中的**原文清单无法逐条比对**；执行侧改以
  **已归档的等价要求**为执行依据（D-032 / D-036 / D-041 与 SESSION-LOG 中 Y-4 的登记，
  以及 X-2 禁改清单），并在回执中逐条注明出处。
- 建议：若参谋侧仍持有 Y 段任务书原文，请补发；执行侧可据以建立 `docs/Y-TASKBOOK.md`
  的正式逐字副本（届时以补发文本为准）。→ **已由 §5 落实。**

## 5. 补发原文
来源标注：**「用户自旧对话复制」**（旧对话中的产出方为当时的参谋侧 AI）。逐字落盘，未改一字。

```
============================================================
R4-V · Y 段任务书（合并终版 · 覆盖此前全部 Y 段补丁）
============================================================
适用范围：本书取代 X 段残余任务与 Y 段此前下发的全部补丁。
执行侧只按本书执行；与本书冲突的旧指令一律作废。
当前停点：Y-3.1（等用户手动暂停）。Y-0 / Y-1 / Y-2 已完成，仅需按本书第一节更正记录。
上游证据：SESSION-LOG 2026-09-22-R4-V.md；X 段回执；Y 段回执 #1。
镜像基线：2e808159634e28c58c90a518ebaf6c5d48eefb68

------------------------------------------------------------
一、Y-2.3 结论更正（先做，改文档不改代码）
------------------------------------------------------------
1.1 作废原结论（含已写入 memoir 的版本）：
    「区间平均 20.2 tick/s、满速 59、占空比约 34%、间歇运行、恢复时刻不可判定」
    作废原因有二：
      a) 时长算错：13:39:59.592 → 20:22:32 实为 24,153 秒，原算式用了 25,953 秒，
         多算 1800 秒，故平均值本身也偏低（实际约 21.7 tick/s）。
      b) 平均值掩盖真实形态：实际是单点解冻，不是间歇运行。

1.2 重算（全部取自已有回执数据，不需新采样）：
    满速标定：18:33:01.404 tick=6,121,704 → 20:22:32 tick=6,509,544
              Δ=387,840 tick / 6,570.6 s = 59.03 tick/s，该区间连续无停顿。
    起跑反推：18:32:49.394 tick=6,120,994 相对 13:39:59.592 的 5,985,107
              Δ=135,887 ÷ 59.03 = 2,302 s = 38 min 22 s
              ⇒ 解冻时刻约 2026-09-23 17:54:27（±30 s）。
    冻结佐证：13:39:59.592 四次调用 tick 仅 5985107→5985110（0.6 秒内 +3，
              每次调用 +1），世界若在跑不可能如此 ⇒ 该时刻仍为冻结态。
    形态结论：13:30 冻结 → 约 17:54:27 单点解冻 → 其后连续满速运行至 20:22:32 仍在跑。
              冻结持续约 4 小时 14 分。

1.3 B-21 撤销（由代码缺陷降为环境事件），改记为：
    ENV-1 机器睡眠导致世界脱离冻结态（环境事件，非代码缺陷）
      现象：如 1.2。
      归因：用户外出期间电源计划进入睡眠，唤醒后世界恢复运行（用户已确认作息吻合）。
            非 bot、非桥接、非 API pause 失效（Y-2.2 grep 自验：执行侧未发过
            resume / set_speed，正样本 IN at= 命中 4 条）。
      结论：pause / resume / set_speed 是 0.4.0 原生节奏控制命令
            （QQBOT-DESIGN 5.2；权限归 AdvanceTime 组），拨动的是游戏自身的暂停状态，
            与游戏内手动暂停等效。清醒状态下已有正路径证据：13:30 pause 成功
            → 13:35:48 冷拷完成（执行侧持续作业）→ 13:39:59 仍冻结，
            即清醒期间冻结至少保持约 10 分钟。
            ⇒ API pause 可靠，本次脱离由环境事件造成。
      防护：Y-3.0 验收窗口期禁睡眠。

1.4 不要与之混淆的开放项（保持原样，不得合并）：
    B-18 get_world_state.paused 字段在 resume 方向读数不可信
         （00:52:56.458 paused=true 而 tick 以 59/s 推进）。属读数字段可靠性问题，
         与暂停能否保持无关。D-033 不变：运行判据一律用 tick。
         另补录正样本：13:30 pause 响应 previous_paused:false / paused:true 与实际吻合，
         矛盾此前只出现在 resume 方向。

1.5 取证教训（写入 SESSION-LOG，供后续轮次引用）：
    a) autosave 目录只保留最近 5 槽，旧槽被游戏修剪 ⇒ 超过 5 个节拍（约 25 分钟）
       的历史无法用 autosave 做时间线取证，需要长时间线时另行留痕。
    b) 报告运行速率时不得只给区间平均值；必须同时给出「是否连续」的判断
       与端点 tick、端点墙钟，否则会把单点事件误述为间歇运行。

1.6 同步：本节更正须覆盖写入 SESSION-LOG、缺陷表、以及执行侧 memoir 中的对应条目，
    旧条目标注作废而非删除。

------------------------------------------------------------
二、Y-3.0 环境前置（不得跳过）
------------------------------------------------------------
Y-3.0a 只读报告当前电源计划：交流电下「关闭显示器」「使睡眠状态」「休眠」三项当前值。
Y-3.0b 提示用户手动把「使睡眠状态」与「休眠」设为从不（验收后可改回）。
       ★ 修改由用户执行，执行侧不得改动系统电源设置。用户回报后复查并贴改后值。
Y-3.0c SESSION-LOG 记录本次验收窗口起止时刻，并写明：
       窗口期内若机器再次睡眠，本轮全部基线作废、重走 Y-3。

------------------------------------------------------------
三、Y-3 冻结与基线（用户操作 + 执行侧复核）
------------------------------------------------------------
Y-3.1 由用户在游戏内手动暂停，回报墙钟时刻与所用方式（暂停按钮 / 空格键 / 其他）。
      采用手动暂停的唯一理由：可与 Y-3.2b 的 autosave 证据相互独立交叉验证，成本极低；
      不再暗示 API pause 不可靠（后续常规轮次可直接由执行侧发 API pause）。
      ★ 采样期间用户不得点击或切换到游戏窗口（排除「窗口交互解除暂停」，
        V-3.0.3b 曾观测到用户点开面板后暂停状态改变）。此项写入 SESSION-LOG。

Y-3.2 冻结复核。每 15 秒读一次 get_world_state，连读 9 次（≥120 秒）。
      每次记录：墙钟（到毫秒）、tick、此前累计 /cmd 调用次数。
      采样期间除这 9 次外不得发任何其他 /cmd。
      三情形判定（必须贴九次全表并写明落入哪一种及依据）：
        情形一 九次 tick 全部严格相等 → 冻结成立，取该 tick 为 TF。
        情形二 tick 每次恰好 +1、总增量等于调用次数减一、与经过秒数无关
               （120 秒只涨 8）→ 判「冻结成立 + 调用步进」，取首次 tick 为 TF，
               登记 B-22，不停报，继续 Y-3.3。
        情形三 增量与经过秒数成正比（20~59 tick/s 量级）→ 世界在跑，按 Y-8(a) 停报。
      ★ 先验：X-0.4 已实测 0.6 秒内四次调用 tick +3，落入情形二的可能性很高。

Y-3.2b 独立佐证（autosave 停摆）。本测试世界 autosave 节拍实测 301~302 秒一槽
      （19:58:58 / 20:03:59 / 20:09:00 / 20:14:00 / 20:19:01）。
      在采样开始时记录 "Mystery and Rain" 最新槽目录名与 mtime；采样结束再记一次；
      再等到距采样开始满 7 分钟记第三次。冻结成立时不应出现新槽。
      出现新槽 → 世界在运行，按 Y-8(a) 停报，且该证据优先于 tick 判定。

Y-3.2c 若落入情形二，登记：
      B-22 桥接 /cmd 调用导致 tick 步进（暂停态下每次命令使 tick +1）。
      影响面：D-033 比较 tick 时须扣除调用次数，或固定用等间隔 get_world_state
      采样并看增量是否与时间成正比；ARCH §5.2 的 C6 实装时同此处理。
      本段只登记，不改判据实现。

Y-3.3 冻结成立后，把该 tick 记为本轮 tick 指纹 TF，在 SESSION-LOG 单列（D-038）。

Y-3.4 冻结态重取四份原文：8724 list_cities_ex、8724 list_armies、
      8723 list_cities、8723 list_kingdoms。贴全文、字节数、tick、墙钟。
      四份 tick 必须等于 TF（若落入情形二，允许按调用次数逐次 +1，须写明）。

Y-3.5 从 Y-3.4 读出城 id 6 的 kingdom_id，记为 KB（D-039：城 6 归谁就绑谁，
      不得预先指定；若城 id 6 已不存在 → 停报）。出具 KB 逐城表：
      city_id | 城名 | 城名 repr() | 人口 | building_count | has_army |
      warrior_slots | max_warriors，并给四项汇总（城数 / 总人口 / 军队数 /
      出兵线 slots-max）。此表为本段 .国情 唯一验收基准，旧基准全部作废。

Y-3.6 记录目标城 id 6 与参照城的 building_count 冻结态初值。
      参照城须选 KB 国内的另一座城（不再用城 7，其属 kingdom 7），给出 city_id 与理由。
      已知 13:39 初值供对照（非验收基准）：城 6=17，另城 1=12 / 2=13 / 3=45 /
      4=4 / 5=15 / 7=20 / 8=10。

Y-3.7 定向备份（D-037）：只拷最小文件集四件
      saves\save10\map.meta / map.wbox / map_stats.s3db / EmpireCraftModData.json
      到 F:\R4V-backups\WorldBox-pre-Y-<时间戳>\，报文件数、总字节数、逐件 SHA256，
      与 X-0.3a.5 所报值比对。拷前报 F: 剩余空间（须 > 待拷体积 3 倍；
      待拷约 17,943,217 B，正常满足）。

------------------------------------------------------------
四、Y-4 代码修复
------------------------------------------------------------
解冻范围（仅此四处，超出即停报）：
  src\qqbot\resolver.py    own 目标解析、错误码三分、B-19 归一化
  src\qqbot\bot.py         _resolve_coords、_h_bind 校验、绑定写入
  src\qqbot\broadcaster.py 仅 :35 一行（B-14）
  src\qqbot\tests\*        用例
禁改：C5 对账门 resolver.py:173-204（逐字不得动）、C4 存活判定、
      ledger.py、executor.py、adapter.py、catalog.py、approval.py、config.local.json。

Y-4.1 作用域与名称拆参（D-032）
  own 路径不得再把 binding.kingdom_id 当名称传入 resolve_city。
  改为先用 kingdom_id 过滤出该国城集合，再在集合内按城名匹配。
  kingdom_id 比较做类型归一（绑定表 TEXT vs 桥返回 int），diff 中指明归一位置与函数。

Y-4.2 三种 own 形态
  a) 我国 <城名>：国内前缀匹配；命中多个走 ARCH §3.4 候选列表，不猜。
  b) 我城（不带城名）：该国城数 == 1 直接命中；> 1 回候选列表要求重发。
     禁止默认取首城或 capital（.造 写持久数据，猜错不可逆）。
  c) 我国首都：直接实装。取 list_kingdoms 中该国 capital_id，
     再在 list_cities_ex 按 id 定位。实测字段存在（22→3、7→7、24→1）。
     capital_id 所指城不在列表中时按 UNSTABLE 处理（先过 C5 对账），不得判 CITY_GONE。
     原「无字段则不上该形态」的退路作废。

Y-4.3 错误码三分（B-17），文案必须可区分，回执贴原文：
  NOT_FOUND / 看不懂这个目标：名称未匹配任何城（含把 id 当城名的历史入参）
  UNSTABLE ：C5 对账未通过（ARCH §5.4，退避 2 s 重拉，最多 3 次）
  CITY_GONE：仅当 C5 对账通过，且 city_id 不再出现在 list_cities_ex 中
             （§5.5 原文：加载不全 ≠ 城消失，防误伤）
  K-2.6b 已实现的两种 0 城 detail 区分不得回退。

Y-4.4 defect E：.bind 存在性校验（撤销「登记不修」）
  a) 走 list_kingdoms（Y 段已实测可用，504 B，字段含 id/name/race/king_name/
     capital_id/cities_count/units_count/wild）。原退路（用 list_cities_ex 的
     kingdom_id 集合）作废，相应注释与缺陷说明一并删除。
  b) 校验失败须区分两种：id 不在 list_kingdoms（不存在）
     与 在其中但 cities_count=0（已无城池）。
  c) 校验通过时把桥返回的国名写入 kingdom_name_snapshot（B-11 快照半）。
  d) ARCH §4.2「国灭则该绑定标记 dead」：本段只做到绑定时校验；
     运行期 dead 标记若未实装，如实登记为缺陷，不在本段展开。

Y-4.5 重绑通路（先取证后实装）
  Y-4.5a 只读取证，先报后改，不得跳过。贴原文与行号：
     1) bot.py 的 _h_bind 全函数
     2) ledger.py 的 bind_async（或等价写入函数）全函数
     3) bindings 表当前全部行（qq 掩码），含 state 列取值
     4) grep「重绑 / rebind / already bound / state」全部命中与行号（0 命中附正样本）
     结论三选一并写明：
       甲 无重绑守卫（直接覆盖）→ 只需 Y-4.4 的存在性校验，.bind <KB> 即可用。
       乙 有守卫且直接拒绝重绑 → 按 Y-4.5b 补 §4.2 dead 通道。
       丙 守卫要求走 God 审批流且该流未实装 → 同样按 Y-4.5b 办，
          不实装通用重绑审批（超范围，登记 B-20 留待 v1.1）。
  Y-4.5b 实装国灭免批重绑（乙 / 丙 情形），依据 ARCH §4.2 原文：
     「绑定时校验 kingdom_id 仍存在（8723 list_kingdoms）；国灭则该绑定标记 dead，
       持有者可 .bind 重绑一次，不消耗点数。」
     a) .bind 到来时先校验当前已绑 kingdom_id 是否仍存在；不存在则把该行 state 置 dead
        （用已有 state 列，禁止改表结构、禁止手改数据库）。
     b) state == dead 者重绑：放行一次，不扣点、不需 God 批；成功后 state 置回 active，
        并按 Y-4.4c 写真实国名快照。
     c) state == active 且目标王国仍存在的普通重绑：按 §3.2 拒绝，
        回执写明「重绑需 God 批，当前未实装」，不得借本通道放行。
     d) 新绑目标本身须先过 Y-4.4 校验；两项校验都过才写库。
     e) 整个重绑写一条 ledger_log（action 标 rebind，cost 0）。
     f) 回执须区分三种结果：原国已灭已放行免批重绑 / 原国仍在需 God 批 /
        目标王国不存在或已无城池。
     ★ 若实装需触碰 approval.py（禁改清单内）→ 按 Y-8(h) 停报。

Y-4.6 B-19 空白归一化。匹配前对候选名与用户输入同时归一
  （U+200A / U+2009 / U+3000 / 连续空白 → 单个普通空格，并去首尾），
  归一只用于比较，回执一律显示桥返回原名。
  ★ 回执文本中 U+200A 会被传递过程吃掉，不得据此认为问题消失；
    必须用 repr() 或逐字节输出复核，实测结果写进缺陷条目。

Y-4.7 B-14：broadcaster.py:35 改为区分 None 与 0，
  文案「军队数：未知（读取失败）」；K-5.2i 快照用例由「锁定缺陷行为」改为「锁定修复后行为」。

------------------------------------------------------------
五、Y-5 回归用例（贴用例源码与行号）
------------------------------------------------------------
 1) binding.kingdom_id 为字符串 KB + 「我国 <城6名>」→ 命中 city id 6
 2) binding.kingdom_id 为整数 KB + 同上 → 命中 city id 6
 3) 城名含 U+200A、用户输入普通空格 → 命中（B-19）
 4) 城名含 U+200A、用户输入无空格 → 命中（B-19）
 5) 「我国 不存在城」→ NOT_FOUND，断言 != CITY_GONE
 6) 「我城」且该国多城 → 候选列表分支，断言不产生任何 invoke
 7) C5 对账不一致 fixture → UNSTABLE，断言 != CITY_GONE
 8) .bind 不存在的 kingdom_id（用 16 与 17 各一条）→ 拒绝，且不写 bindings
 9) .bind 存在的 kingdom_id → 通过，kingdom_name_snapshot 为真实国名
10) army_count 为 None → 回执「军队数：未知（读取失败）」（B-14，改写 K-5.2i）
11) 进程内实测：resolve_city(str(KB)) 的旧路径不再可达
12) 已绑不存在的王国 + .bind KB → 放行，cost=0，state 由 dead 回 active，
    快照为桥返回真实国名
13) 已绑一个仍存在的王国 + .bind 另一个仍存在的王国 → 拒绝，bindings 逐字不变，
    回执含「需 God 批」
14) 已绑不存在王国 + .bind 另一个同样不存在的 id → 拒绝，state 保持 dead，不写库
15) 「我国首都」→ 命中 capital_id 所指城
16) capital_id 所指城不在 list_cities_ex 中 → UNSTABLE，断言 != CITY_GONE
计数下限 ≥ 173（159 基线 + 本段 ≥14 条）。
★ fixture 一律用 Y-3.4 冻结态原文按 K-5.1 同法逐字节落库；旧 fixture 保留不删，
  改名标注所属历史世界。禁止手写 {"ok":true} 之类的桩
  （R4-2c 的 47 绿与 K 段的 147 绿两次教训）。

------------------------------------------------------------
六、Y-6 验收与提交
------------------------------------------------------------
pytest 连跑 3 次 ≥173 全绿，贴三次计数与耗时。
git diff 仅限第四节解冻清单；resolver.py:173-204 对账门在 diff 中逐字未出现。
git status clean 后提交，message 形如
「R4-2k: fix B-16/B-17/B-19/defect E/B-14 + capital form (N tests)」，回 40 位 SHA。
重启 bot 前后各报一次 pending_approval 行数，必须为 0（D-030 红线）。
停机一律用 _r4v_stopbot.py v2（缺陷 F 修法），禁止强杀游戏进程。

------------------------------------------------------------
七、Y-7 联机验收（每步前后读 tick，须等于 TF）
------------------------------------------------------------
Y-7.1 发 .bind <KB>：贴入站原文、出站回执原文、bindings 改前改后行原文（qq 掩码）。
      断言 kingdom_name_snapshot 为桥返回原名（不归一）、points 余额不变（免点数重绑）。
      若被拒 → 停报，贴回执原文与 Y-4.5a 的结论编号（甲/乙/丙）。
Y-7.2 发 .国情：四项与 Y-3.5 表逐项一致才算过，贴回执原文与对照表。
Y-7.3 发「我国首都」形态一次（只读路径），贴回执原文。
Y-7.4 发「我城」形态一次：KB 若多城，预期候选列表分支，断言无 invoke。
Y-7.5 造墙：.造 wall_iron，目标城 id 6。脚本提前待命，先捕首条 OUT
      （含 IN→OUT 间隔与墙钟）。catalog 实测 wall_iron price=1 /
      city_cooldown_sec=0 / personal_cooldown_sec=60；七种墙功能等价仅外观不同
      （QQBOT-DESIGN 7.4）。
Y-7.6 提示用户在首条 OUT 后 60 秒内发第二条同样指令，预期被个人冷却拒绝并明示原因。
Y-7.7 效果只看 building_count（8723 list_cities）：目标城 id 6 相对 Y-3.6 初值应 +N，
      参照城应逐字不变。世界冻结，不看人口。
Y-7.8 事务四表（points / personal_cooldown / city_cooldown / ledger_log）
      在两条指令前后各取一次行原文与行数。
      ★ points 此前 ROWS=0，本次为 D-031 正路径首次验证：
        首次入账必须 30，造墙后 balance 必须 29；出现 5 立即停报。
      ★ city_cooldown 必须保持 0 行（QQBOT-DESIGN 6.4 建设类不吃城市冷却）。

------------------------------------------------------------
八、Y-8 停报项
------------------------------------------------------------
 a) Y-3.2 落入情形三，或 Y-3.2b 出现新 autosave 槽（冻结未保持）
 b) 任一步骤读到的 tick 不等于 TF（情形二的调用步进除外，须写明）
 c) 冻结态下城 id 6 不存在
 d) 修复后「我国 <城名>」「我城」「我国首都」任一形态返回 CITY_GONE
 e) points 首次入账不是 30，或造墙后 balance ≠ 29
 f) city_cooldown 新增行
 g) 造墙 accepted 但目标城与参照城 building_count 均无变化
 h) 需要改动第四节禁改清单中的任何文件（含 Y-4.5b 触碰 approval.py）
 i) 备份校验不一致
 j) 任一盘剩余空间低于 5 GB
 k) 验收窗口内机器再次睡眠

------------------------------------------------------------
九、硬约束（全段适用）
------------------------------------------------------------
凭据零回显（汇报、文档、脚本回显、变量打印、报错信息一律不得出现）。
不手改数据库；不删除任何备份目录与存档文件；不强杀游戏进程；
不自行安装计划外组件；不跳闸门；不使用省略号；
grep 得 0 命中必须附正样本自验；
实测与任务书断言不符时以实测为准并如实报差异（AGENT-CONTRACT 常设条款）。
收尾按补充裁决 B-1/B-2：本地提交 → 脱敏闸门 → 写 SESSION-LOG 与 RUNNER-HANDOFF
→ 推镜像 → 对话侧只回 15 行以内摘要 + 固定 SHA 直链，完整回执落盘不贴进对话。
============================================================
```

## 6. 记忆，未核实（单列，不混入原文节）
**本节为空**：本次补发文本中**没有任何条目**被标为「记忆，未核实」。
另如实注明（不属该节、仅供辨识）：§5 的补发文本自带元数据（如「镜像基线
2e808159634e28c58c90a518ebaf6c5d48eefb68」「当前停点：Y-3.1」）为**其自身内容**，
执行侧未对其真实性作判定；实际执行时点与镜像基线以 SESSION-LOG 记录的实测为准。
